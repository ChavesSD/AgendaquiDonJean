# Configurações do servidor
PORT=3000
NODE_ENV=development

# MongoDB Atlas (CONFIGURE SUAS CREDENCIAIS)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT (GERE UMA CHAVE SEGURA E ÚNICA)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS (CONFIGURE SEU DOMÍNIO)
CORS_ORIGIN=http://localhost:3000

# Railway (apenas para produção)
RAILWAY_STATIC_URL=https://your-domain.railway.app

# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Substitua todas as credenciais pelas suas
# 3. Gere uma chave JWT segura e única
# 4. Configure o domínio correto
# 5. NUNCA commite o arquivo .env com credenciais reais
