# Configurações para produção no Railway
PORT=3000
NODE_ENV=production

# MongoDB Atlas (CONFIGURE SUAS CREDENCIAIS AQUI)
# Formato: mongodb+srv://usuario:senha@cluster.mongodb.net/nome_do_banco?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/domjeanbanco_db_user?retryWrites=true&w=majority

# JWT (GERE UMA CHAVE SEGURA E ÚNICA)
JWT_SECRET=your-super-secret-jwt-key-here

# CORS (CONFIGURE SEU DOMÍNIO DO RAILWAY)
CORS_ORIGIN=https://your-domain.railway.app

# Railway
RAILWAY_STATIC_URL=https://your-domain.railway.app

# IMPORTANTE: 
# 1. Substitua todas as credenciais acima pelas suas
# 2. Gere uma chave JWT segura e única
# 3. Configure o domínio correto do Railway
# 4. NUNCA commite este arquivo com credenciais reais
